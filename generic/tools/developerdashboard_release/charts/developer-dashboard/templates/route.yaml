{{- if eq .Values.clusterType "openshift" }}
apiVersion: v1
kind: Route
metadata:
  name: {{ .Values.host }}
spec:
  to:
    kind: Service
    name: {{ include "developer-dashboard.fullname" . }}
  tls:
    termination: Reencrypt
{{- end }}